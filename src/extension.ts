import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Pi composition root. Runtime registration is added in vertical capability slices. */
export default function piCodexAdaptor(_pi: ExtensionAPI): void {}
